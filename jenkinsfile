pipeline {
    agent any

    tools {
        maven "3.6.3"
        ansible
    }

    stages {
        stage("Build") {
            steps {
                sh "mvn -version"
                sh "mvn clean install"
            }
        }
        stage("Deploy ap to vm"){
             steps{
                  sh "which ansible"
             }
        }
    }
}